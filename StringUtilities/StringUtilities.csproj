<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>netcoreapp2.1</TargetFramework>
    <AssemblyName>StringUtilities</AssemblyName>
    <RootNamespace>StringUtilities</RootNamespace>
    <Authors>Mattia Marchiani</Authors>
    <Company>Pomiager srl</Company>
    <Description>Extention to String class that makes it easier to do some commons jobs as check if string contains at least one value from an array, check if ends with at least one value from an array, get the file extension of a file name, compute the hash of a string, encrypt and decrypt the string with RSA and more.</Description>
  </PropertyGroup>

</Project>
